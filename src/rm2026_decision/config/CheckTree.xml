<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4">
  <BehaviorTree ID="CheckTree">
    <Sequence>
      <CostmapCheckGoal ros_node="{@ros_node}"
                        game_progress="{@game_progress}"
                        nav_goal="{@nav_goal}"
                        costmap="{@costmap}"
                        current_pose_x="{@current_pose_x}"
                        current_pose_y="{@current_pose_y}"
                        nav_goalOut="{@nav_goal}"
                        nav_goal_requested="{@nav_goal_requested}"
                        self_save_statusOut="{@self_save_status}"
                        cmd_vel_saveOut="{@cmd_vel_save}"/>
    </Sequence>
  </BehaviorTree>
  <TreeNodesModel/>
</root>
